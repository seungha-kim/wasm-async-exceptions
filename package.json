{
  "name": "learn-asyncify",
  "private": true,
  "version": "0.0.0",
  "description": "Emscripten Asyncify × Exception pitfalls research",
  "scripts": {
    "build:examples": "scripts/build-all-examples.sh",
    "serve": "http-server . -p 8080 -c-1 --silent",
    "test": "npm run build:examples && playwright test --workers=1"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^26.1.0",
    "http-server": "^14.1.1",
    "typescript": "^5.5.0"
  }
}
